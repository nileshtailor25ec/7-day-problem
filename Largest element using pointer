#include <iostream>
using namespace std;
int main() {
    int n;
    cout << "Enter number of elements: ";
    cin >> n;
    int arr[n];
    cout << "Enter " << n << " elements: ";
    for(int i = 0; i < n; i++) 
    cin >> arr[i];
    int *ptr = arr, max = *arr;
    for(int i = 1; i < n; i++) {
    if(*(ptr + i) > max)
    max = *(ptr + i);
    }
    cout << "Largest element: " << max << endl;
    return 0;
}

Result-Enter number of elements: 4
Enter 4 elements: 12 45 7 23
Largest element: 45
